import * as React from 'react';

export function Logo({ className, ...props }: React.ImgHTMLAttributes<HTMLImageElement>) {
  // Returns null to satisfy user request for a text-only logo
  return null;
}
